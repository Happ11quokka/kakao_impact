# 아보하 프론트엔드 환경변수 템플릿 (Vite)
# 로컬: `.env.local` 로 복사해 사용.

# FastAPI 백엔드 URL. 프로덕션에선 Railway 의 backend 서비스 Public URL.
VITE_API_URL=http://localhost:8000

# localhost + npm run dev에서만 사용하는 개발 편의 옵션.
# 배포 환경에서는 설정하지 않는다.
VITE_DEV_AUTH=false
VITE_USE_MOCK_API=false
